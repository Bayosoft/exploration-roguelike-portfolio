namespace ExplorationRoguelike
{
    // NOTE: Ids are specified so that it doesn't default to a different name when stuff is moved around
    // DO NOT CHANGE IDs.
    public enum SpeakerName
    {
        SYSTEM = -1,
        PLAYER = 0,
    }
}
